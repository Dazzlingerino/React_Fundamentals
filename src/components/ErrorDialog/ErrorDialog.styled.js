import styled from 'styled-components';

export const ErrorContainer = styled.div`
	.ok-button {
		position: relative;
		left: 90%;
	}

	.error-dialog {
		text-align: center;
		padding: 24px 0 24px;
		max-width: 100%;
		font-size: 20px;
		font-weight: bold;
	}

	.error {
		color: red;
		margin-top: 10px;
		font-size: 24px;
		font-weight: normal;
	}
`;
